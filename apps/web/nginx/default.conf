server {
  listen 3000;
  server_name _;

  root /usr/share/nginx/html;

  location = /healthz {
    return 200 "ok";
  }

  location = /EAS {
    return 301 /EAS/;
  }

  # Static SPA
  location /EAS/ {
    try_files $uri $uri/ /EAS/index.html;
  }
}
